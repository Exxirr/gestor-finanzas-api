spring:
  application:
    name: gestor-finanzas

  datasource:
    url: jdbc:mysql://localhost:3306/finanzas_db
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  server:
    port: 8080



